# Reviewed first full-data candidate configuration. Supply an approved external release manifest with --manifest-path.
schema_version: "1.0.0"
experiment_name: "navigation-mvp-full-candidate"
dataset:
  yaml_path: "dataset.yaml"
  inspection_report_path: null
  stage: "released"
model:
  architecture_path: null
  initial_weights_path: "ML_side/models/yolo26n.pt"
training:
  epochs: 20
  image_size: 640
  batch_size: 16
  device: "auto"
  workers: 0
  seed: 42
  optimizer: "AdamW"
  learning_rate: 0.001
  confidence: 0.001
  iou: 0.7
  deterministic: true
  fraction: 1.0
  val: true
  resume_behavior: "never"
output:
  root: "ML_side/artifacts/navigation_mvp"
notes: "Reviewed first full-data navigation-model candidate run. Batch 16 was selected from real RTX 3050 calibration evidence: 2.66G reported GPU memory and materially better throughput than batch 24. Uses the full approved training split for 20 epochs with validation enabled. Output remains a candidate requiring formal validation and held-out evaluation before any promotion."
